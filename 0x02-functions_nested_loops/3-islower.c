#include"main.h"
/**
 * _islower - write a function that checks the lowercase character
 * @c: is a integer
 * return 0 or 1
*/
/**
* return value 1 or 0 '_islower'
*/
int _islower(int c)
{
if (c <= 122 && c >= 97)
return (1);
else
return (0);
}
